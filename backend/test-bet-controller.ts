// import { Request, Response } from 'express';
// import { DatabaseAdapter } from '../db/DatabaseAdapter';
// import { Bet, CreateBetDto, MatchType } from '../types/models';

export const getUserBets = async (req: any, res: any): Promise<void> => {
    res.json({ message: "test" });
};

export const getBetsByMatch = async (req: any, res: any): Promise<void> => {
    res.json({ message: "test" });
};

export const createOrUpdateBet = async (req: any, res: any): Promise<void> => {
    res.json({ message: "test" });
};

export const deleteBet = async (req: any, res: any): Promise<void> => {
    res.json({ message: "test" });
};
